{
  "name": "real-estate-wholesale-os",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build",
    "lint:web": "npm --prefix apps/web run lint",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload --reload-exclude '.venv/*' --host 0.0.0.0 --port 8000",
    "test:api": "cd apps/api && uv run pytest",
    "lint:api": "cd apps/api && uv run ruff check .",
    "typecheck:api": "cd apps/api && uv run mypy app tests",
    "bootstrap:api": "cd apps/api && uv run python -m app.cli.bootstrap",
    "seed:demo": "cd apps/api && uv run python -m app.cli.seed_demo",
    "db:migrate": "cd apps/api && uv run alembic upgrade head",
    "db:backup": "bash scripts/db-backup.sh",
    "db:restore-verify": "bash scripts/db-restore-verify.sh",
    "documents:migrate": "cd apps/api && uv run python -m app.cli.migrate_private_files",
    "ops:alert-test": "bash scripts/operations-alert-test.sh",
    "ops:smoke": "bash scripts/smoke-test.sh",
    "worker": "cd apps/worker && uv run python -m worker.main"
  },
  "workspaces": [
    "apps/web"
  ]
}
